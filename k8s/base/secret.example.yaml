# Example only — do NOT commit real values. Apply your own Secret with the
# same name, or use a SealedSecret / ExternalSecret / sops in production.
apiVersion: v1
kind: Secret
metadata:
  name: bridge-secrets
type: Opaque
stringData:
  DB_USER: bridge
  DB_PASSWORD: changeme
